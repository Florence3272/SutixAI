<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.sutix</groupId>
        <artifactId>sutix-backend</artifactId>
        <version>1.0.0</version>
    </parent>

    <modelVersion>4.0.0</modelVersion>
    <artifactId>sutix-admin</artifactId>
    <name>sutix-admin</name>
    <description>启动模块</description>

    <dependencies>
        <!-- 依赖所有业务模块 -->
        <dependency>
            <groupId>com.sutix</groupId>
            <artifactId>sutix-common</artifactId>
            <version>1.0.0</version>
        </dependency>
        <dependency>
            <groupId>com.sutix</groupId>
            <artifactId>sutix-system</artifactId>
            <version>1.0.0</version>
        </dependency>
        <dependency>
            <groupId>com.sutix</groupId>
            <artifactId>sutix-term</artifactId>
            <version>1.0.0</version>
        </dependency>
        <dependency>
            <groupId>com.sutix</groupId>
            <artifactId>sutix-corpus</artifactId>
            <version>1.0.0</version>
        </dependency>
        <dependency>
            <groupId>com.sutix</groupId>
            <artifactId>sutix-translation</artifactId>
            <version>1.0.0</version>
        </dependency>

        <!-- SpringBoot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mybatis</groupId>
            <artifactId>mybatis-spring</artifactId>
            <version>2.1.2</version>
            <scope>compile</scope>
        </dependency>

        <!-- MySQL驱动（生产环境必须） -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>  <!-- runtime表示运行时才需要 -->
        </dependency>
    </dependencies>

    <!-- 1. 打包类型必须是jar -->
    <packaging>jar</packaging>

    <build>
        <plugins>
            <!-- 2. 必须包含SpringBoot打包插件 -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring.boot.version}</version>
                <configuration>
                    <mainClass>com.sutix.admin.AdminApplication</mainClass>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>