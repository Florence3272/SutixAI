spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sutix_term_platform?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: shs
    password: 20051024
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      lettuce:
        pool:
          max-active: 8 # 最大连接数
          max-idle: 8 # 最大空闲连接
          min-idle: 0 # 最小空闲连接
  cors:
    allowed-origins: "*"
    allowed-methods: GET,POST,PUT,DELETE,OPTIONS
    allowed-headers: "*"

# ========== trans-home 文档翻译 API 配置 ==========
trans-home:
  host: http://translate.codeai.net.cn
  token: 0QWEaOmqpVW1jI8i
  # 单位：毫秒，上传/下载超时
  connect-timeout: 15000
  read-timeout: 60000

server:
  port: 8080
  servlet:
    context-path: ""

mybatis-plus:
  mapper-locations: classpath*:com/sutix/**/mapper/*.xml
  type-aliases-package: com.sutix.*.entity